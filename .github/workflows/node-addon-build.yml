name: Build Node.js Addon

on:
  push:
    branches: [ main, 0.0.9 ]
  pull_request:
    branches: [ main ]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Install dependencies
      run: npm install
    
    - name: Build native addon
      run: npm run build
    
    - name: Test
      run: node test.js

  build-macos:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Install dependencies
      run: npm install
    
    - name: Skip build (Windows-specific code)
      run: echo "Skipping build on macOS - code is Windows-specific"
    
    - name: Skip test (Windows-specific code)
      run: echo "Skipping test on macOS - code is Windows-specific"

  build-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Install dependencies
      run: npm install
    
    - name: Skip build (Windows-specific code)
      run: echo "Skipping build on Linux - code is Windows-specific"
    
    - name: Skip test (Windows-specific code)
      run: echo "Skipping test on Linux - code is Windows-specific"
